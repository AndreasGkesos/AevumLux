{
  "name": "cc-expired-tokens",
  "description": "Access tokens are issued with a 5 second lifetime, so they are already expired by the time you inspect them in AevumLux. Demonstrates Token Validator's expiry check failing on an otherwise-valid token.",
  "accessTokenLifetime": "00:00:05",
  "clients": [
    {
      "clientId": "cc-expired-tokens",
      "clientSecret": "expired-tokens-secret",
      "displayName": "CC - Expired Tokens",
      "allowedScopes": [ "api" ]
    }
  ]
}
