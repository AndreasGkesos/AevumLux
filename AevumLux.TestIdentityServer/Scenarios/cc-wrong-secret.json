{
  "name": "cc-wrong-secret",
  "description": "The client is registered on the server with a different secret than the one AevumLux's seeded provider will send. Demonstrates the invalid_client error a mismatched secret produces.",
  "clients": [
    {
      "clientId": "cc-wrong-secret",
      "clientSecret": "server-side-secret-that-does-not-match",
      "displayName": "CC - Wrong Secret",
      "allowedScopes": [ "api" ]
    }
  ]
}
